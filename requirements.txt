# Versiones fijadas (==): son las verificadas con la suite de tests y con las
# corridas reales de evidencia/ (Python 3.12).

pydantic==2.13.5
python-dotenv==1.2.3
langchain-core==0.3.86
langchain-text-splitters==0.3.11
langchain-openai==0.2.14
langchain-anthropic==0.3.22
langchain-ollama==0.2.3
# chromadb 1.x reescribió su core en Rust (ya no depende de compilar
# chroma-hnswlib en C++): evita necesitar Visual Studio Build Tools en
# Windows para instalar, que era un problema con chromadb 0.x.
langchain-chroma==0.2.6
chromadb==1.5.9
# Embeddings locales (indexación y consulta, src/ingest.py::_build_embeddings):
# HuggingFaceEmbeddings("sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"),
# corre 100% local (descarga el modelo una vez desde el Hub y lo cachea), sin
# API key. Variante multilingüe (no all-MiniLM-L6-v2, casi solo inglés) porque
# los documentos y preguntas de este proyecto están en español.
langchain-huggingface==0.3.1
sentence-transformers==3.4.1
# Tokenizer del modelo de embeddings: el chunking mide en tokens (src/ingestion.py).
transformers==4.57.6

# pytest acá también igual que en criterio que los
# Módulos 1 y 2: un `pip install -r requirements.txt` + `pytest` "a secas"
# alcanza para correr la suite.
pytest==8.4.2
pytest-asyncio==0.26.0
